import { StyleSheet } from 'react-native';

const stylesFn = () =>
  StyleSheet.create({
    container: {
      margin: '5%',
    },
    form: {
      height: 25,
      width: '100%',
    },
  });

export default stylesFn;
