/* import { get, set, remove } from '../../utilities/async_storage';

const INVENTORY_STORAGE_KEY = 'INVENTORY_STORAGE_KEY';

export const load = async () => {
  return await get(INVENTORY_STORAGE_KEY);
};

export const save = async (customers) => {
  return await set(INVENTORY_STORAGE_KEY, customers);
};

export const clear = async () => {
  return await remove(INVENTORY_STORAGE_KEY);
};
 */
